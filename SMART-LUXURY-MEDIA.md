# Smart Luxury — media status

Update: the page now uses `Luxury-1.png`, `Luxury-2.png`, and `Luxury-3.png` (in `public/Smart-Luxury/`) for all of its photography — the two-panel "Two rooms" spread and the full-bleed bedroom band. The original plan called for 6 separate photos across three sections; those sections were simplified down to fit the 3 images actually supplied, so the prompts that used to be here for `scene-lighting.jpg`, `scene-shades.jpg`, `scene-screens.jpg`, `band-theater.jpg`, `diptych-detail.jpg`, and `diptych-wide.jpg` are no longer needed and have been removed from this file.

## Still outstanding

**`hero-poster.jpg`** (goes in `public/Smart-Luxury/`) — the poster frame for the hero video (`public/Luxury-Hero.mp4`, already in place). Should be a single frame pulled directly from that video (e.g. the first frame) so the static poster matches the video exactly before playback starts. Until it exists, the hero falls back to the `bg-navy-deep` section background, which is fine but not as polished as a matching first-frame poster.

Nothing else is needed — the page is fully art-directed with what's already in `public/Smart-Luxury/` and `public/Luxury-Hero.mp4`.
